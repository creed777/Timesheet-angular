import { ClientStatusModel } from './client-status-model';

describe('ClientStatusModel', () => {
  it('should create an instance', () => {
    expect(new ClientStatusModel()).toBeTruthy();
  });
});
